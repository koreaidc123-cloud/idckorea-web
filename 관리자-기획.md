# 관리자 화면 — 기획서

작성 2026-08-17 · 영태리 현장에서 랙PC 3대 등록을 마친 뒤 정리했습니다.

---

## 이 문서가 왜 있나

지금까지 대표님 한 분이 엑셀로 하시던 일을 화면이 대신하게 만드는 것이 목표입니다.
**기능 목록이 아니라 "하루 업무 순서"를 기준으로 화면을 짭니다.**

### 지금까지의 수동 업무 (이걸 없애는 것이 목적)

```
고객이 여러 창구로 들어옵니다
  홈페이지 결제 → 연락        스마트스토어 결제 → 연락
  카톡 문의 먼저 → 결제        무통장입금 (입금자 ≠ 사용자)
        ↓
  엑셀에 손으로 적습니다
  IP · 애니데스크 번호 · 비밀번호 · 고객명 · 임대기간
        ↓
  만료가 다가오면 한 명씩 연장 여부를 묻습니다
        ↓
  연장 안 하면 → 회수 → 포맷 → 애니데스크 비밀번호 새로 설정 → 재투입
```

**문제가 생기는 지점**

| 무엇 | 왜 힘든가 |
|---|---|
| 창구가 여러 개 | 어디서 온 문의인지, 결제는 됐는지 매번 찾아봐야 함 |
| 입금자 ≠ 사용자 | 문의가 와도 누구 건지 바로 안 나옴 |
| 엑셀 수기 | 대수가 늘수록 사람이 감당 못 함. 실수가 바로 사고 |
| 만료 관리 | 놓치면 매출 빠짐. 챙기면 사람이 갈림 |
| PC 잔고장 | 재부팅 한 번이면 될 일에 사람이 서버실까지 감 |

---

## 우선순위 (대표님이 정하신 순서)

| 순위 | 무엇 | 지금 상태 |
|---|---|---|
| 1 | 랙맵 + 실시간 PC 연동 | ✅ **완료** (2026-08-17) |
| 2 | 결제 즉시 애니데스크·비밀번호 자동 배정 | 코드 완성 · **토스 심사 대기** |
| 3 | 알리고 LMS·알림톡 → 만료 연장 자동 안내 | 미착수 |
| 4 | CS 응대 편의 (상담창구 통합) | 미착수 |

2번은 코드가 이미 다 있습니다. **토스 가맹 심사만 통과하면 그날 바로 켜집니다.**
그래서 심사 서류가 지금 가장 급한 병목입니다.

---

## 화면 재설계

### 지금의 "오늘 할일" 이 왜 안 맞나

지금 대시보드는 **숫자를 보여주는 화면**입니다. 총 몇 대, 매출 얼마.
그런데 대표님이 아침에 켜서 알고 싶은 건 그게 아니라 **"오늘 내가 뭘 해야 하나"** 입니다.

### 바꿀 모습 — 처리할 일만 위에서부터

```
┌─────────────────────────────────────────────┐
│  🔴 지금 문제 있는 PC          3대          │
│     Y-014  신호 끊김 12분   [재부팅] [상세] │
│     Y-031  꺼짐 2시간       [재부팅] [상세] │
│     D-007  점검중 표시       [상세]         │
├─────────────────────────────────────────────┤
│  🟠 결제됐는데 배정 안 됨      1건          │
│     홍길동 · 3700X · 10:22 결제  [배정]     │
├─────────────────────────────────────────────┤
│  🟠 오늘·내일 만료             4건          │
│     [선택 발송] 으로 연장 안내 한 번에       │
├─────────────────────────────────────────────┤
│  🟡 검증 대기 (현장 등록)      2대          │
│     Y-004 5-4-12  [접속 확인 완료 → 판매]   │
├─────────────────────────────────────────────┤
│  🟡 회수 대상 (만료 지남)      1대          │
│     Y-009  3일 지남  [회수 → 포맷 대기]     │
└─────────────────────────────────────────────┘
```

**할 일이 없으면 "오늘은 처리할 일이 없습니다" 한 줄만 뜹니다.**
숫자 통계는 아래로 내리거나 별도 화면으로 뺍니다.

---

## 새로 만들 기능

### 1. PC 원격 재부팅 ★

가장 효과가 큰 기능입니다. **간단한 오류로 서버실까지 가는 일이 없어집니다.**

**어떻게 되나** — 랙PC 는 2분마다 데이터베이스로 신호를 보냅니다(나가는 방향만).
그 응답에 명령을 실어 보내면 PC 가 받아서 실행합니다. 포트를 열 필요가 없습니다.

```
관리자 [재부팅]
   → pcs.cmd = 'reboot' 로 기록
   → 다음 신호 때 beat() 가 명령을 돌려주고 즉시 지움
   → 에이전트가 shutdown /r 실행 (SYSTEM 권한이라 가능)
   → 최대 2분 안에 재부팅
```

**안전장치 (반드시 같이)**
- 확인창 필수
- `in_use = true` (지금 누가 접속 중) 이면 **빨간 경고**를 먼저 띄움
- 누가 언제 눌렀는지 `view_log` 에 기록
- 명령은 1회용. 실행되면 바로 지워져서 재부팅 반복이 안 됨

**나중에 같은 방식으로 추가 가능** — 애니데스크 재시작 · 비밀번호 변경 · 사양 재수집

### 2. 이력 조회

**데이터는 이미 다 쌓이고 있습니다.** `orders` 에 어느 PC를 · 누가 · 언제부터 언제까지 ·
어떻게 끝났는지가 전부 남습니다. 해지해도 줄이 지워지지 않습니다.

문제는 화면이 **진행 중인 주문만 불러온다**는 것입니다.

```js
const where = all ? '' : '&status=in.(paid,assigned,ready)';   // 끝난 주문을 안 가져옴
```

서버는 `all=1` 을 이미 지원합니다. 화면이 안 쓸 뿐입니다.

**만들 것**
- PC 상세에 **"이 PC를 거쳐간 고객"** 목록 (기간·금액·종료사유)
- 회원 상세에 **"지금까지 쓰신 내역"** 전체
- 주문·회원 화면에 **기간 선택(달력)** — 지금은 날짜 입력칸이 한 개도 없습니다

> ⚠️ **PC 를 지울 때 `orders.pn` 을 비우면 이력이 끊깁니다.**
> PC 는 삭제하지 말고 `retired`(퇴역) 상태로 두어야 합니다. 이 상태값을 새로 만듭니다.

### 3. 고객 찾기 — 입금자 ≠ 사용자 대응

문의가 왔을 때 **어느 값으로 검색하든 한 번에 나와야** 합니다.

```
검색창 하나 :  이름 · 전화번호 · 입금자명 · 품번 · 애니데스크 번호 · 주문번호
               → 어느 것으로 쳐도 그 고객과 PC 가 같이 나옴
```

`orders` 에 `payer_name`(입금자)과 `user_name`(실사용자)이 이미 따로 있고,
다르면 관리자 화면에 **⚠ 명의다름** 배지가 뜨게 되어 있습니다. 검색만 붙이면 됩니다.

### 4. 알림 선별 발송

**지금 알림 화면은 전부 흉내입니다.** 발송 상태를 줄 번호로 지어냅니다.

```js
const st = i%5===0 ? 'wait' : (o.via==='google' && i%3===0) ? 'sms' : 'ok';
```

**만들 것**
- 대상 줄마다 **체크박스** → 고른 사람에게만 발송
- 알리고(또는 솔라피) 연동 — 알림톡 우선, 실패 시 LMS 자동 전환
- 템플릿 3종: 만료 D-5 안내 · 접속정보 전달 · 연장 완료
- 발송 기록 저장 (누구에게 언제 무엇을)

> 대상 선정에 버그도 있습니다. `o.left` 가 비면 `null <= 5` 가 참이 되어
> 만료일 없는 주문까지 대상에 들어갑니다.

### 5. 만료 자동 처리

**지금은 기간이 끝나도 아무 일도 일어나지 않습니다.** `ends_at` 이 지나도
`pcs.status` 를 바꾸는 코드가 없습니다. 주문 화면에서 "만료 N건" 이라고 세어 보여줄 뿐입니다.

그대로 두면 **PC 가 영영 "임대중" 으로 묶여 다시 안 팔립니다.**

```
기간 종료
  → 주문 status = expired
  → PC status = 회수대기
  → 대시보드 "회수 대상" 에 등장
  → 회수·포맷 후 재등록하면 검증대기로 복귀
```

### 6. 재등록 안전장치

포맷 후 다시 등록할 때 지금은 **상태가 그대로 남습니다.**

| 경우 | 지금 | 고칠 것 |
|---|---|---|
| 해지 안 하고 포맷 | `rent` 유지 → 영영 안 팔림 | 검증대기로 |
| 해지 후 포맷 | `ok` 유지 → 확인 없이 바로 판매 | 검증대기로 |
| 임대중인데 재등록 | 그냥 됨 → 고객 접속 끊김 | **거부** + 안내 |

### 7. 확인창

지금 `confirm()` 이 관리자 전체에서 **"랙 삭제" 한 곳뿐**입니다.
아래는 누르는 즉시 서버에 반영됩니다.

- PC 수정 저장 (품번 · 애니데스크 번호 · 비밀번호 · **위치**)
- 검증 승인 / 실패 / 점검 전환
- 운영 설정 저장
- **재부팅** (새로 만들 것)

되돌리기 번거로운 것부터 확인창을 붙입니다.

### 8. 화면 터짐 방지

`o.pc.pn` 을 널 체크 없이 쓰는 곳이 **9군데**입니다.
PC 가 연결되지 않은 주문이 하나만 있어도 **그 화면 전체가 하얗게 뜹니다.**

---

## 상담창구 통합 — 현실적인 답

카카오톡 채널 상담과 네이버 스마트스토어 톡톡을 **직접 붙이는 것은 큰 일**입니다.
양쪽 다 파트너 승인 절차가 있고, 승인 조건과 요금이 수시로 바뀝니다.

**권하는 방법**: 채널톡·해피톡 같은 **상용 통합 상담 서비스**를 쓰는 것입니다.
이미 카카오 상담톡과 네이버 톡톡을 하나의 화면으로 묶어 주고, 관리자에는 위젯 한 줄만 넣으면 됩니다.

```
고객 문의 (카톡 · 톡톡 · 홈페이지)
        ↓
   통합 상담 서비스 (채널톡 등)
        ↓
   관리자 화면 안에서 응대 + 고객 검색으로 품번 즉시 확인
```

직접 개발보다 **비용도 시간도 훨씬 적게 듭니다.** 대수가 커지고 상담량이 많아지면
그때 자체 개발을 검토해도 늦지 않습니다.

> 각 서비스의 현재 요금·연동 조건은 계약 전에 직접 확인이 필요합니다.

---

## 진행 순서

### 1단계 · 안전 (작음, 먼저)
- [ ] 화면 터짐 방지 (`o.pc` 널 체크 9곳)
- [ ] 주요 동작 확인창

### 2단계 · 재부팅 ★
- [ ] `pcs` 에 `cmd` · `cmd_at` 칸 추가
- [ ] `beat()` 가 명령을 돌려주고 지우도록
- [ ] 에이전트가 명령을 받아 실행 (재시도 로직도 같이)
- [ ] 관리자 [재부팅] 버튼 + 경고 + 기록

### 3단계 · 대시보드 재설계
- [ ] "오늘 할일" 을 처리 대상 중심으로
- [ ] 문제 PC 분류 + 바로 조치

### 4단계 · 이력·검색
- [ ] 끝난 주문까지 불러오기
- [ ] PC별 · 회원별 이력 화면
- [ ] 기간(달력) 검색
- [ ] PC `retired` 상태

### 5단계 · 운영 필수 (첫 결제 전까지)
- [ ] 만료 자동 처리
- [ ] 재등록 안전장치

### 6단계 · 알림
- [ ] 체크박스 선별 발송
- [ ] 알리고 연동 (대표님 가입 필요)
- [ ] 발송 기록

### 7단계 · 상담 통합
- [ ] 통합 상담 서비스 선정 후 위젯 적용

---

## 대표님이 하셔야 진행되는 것

| 무엇 | 왜 |
|---|---|
| **토스 가맹 심사** | 2순위(자동 배정)가 여기서 막혀 있습니다. **가장 급합니다** |
| 알리고(또는 솔라피) 가입 | 알림 발송 연동에 필요 |
| 통합 상담 서비스 선택 | 상담창구 통합에 필요 |
| 카카오 JavaScript 키 | 지금 비어 있어 카카오 로그인이 안 됩니다 |
