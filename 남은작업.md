# 한국 가상컴 — 남은 작업  (2026-08-18 기준)

지금 상태를 `/api/health` 로 언제든 확인하실 수 있습니다.
→ https://idckorea-dusky.vercel.app/api/health

이 문서는 **대표님이 하실 것**과 **제가 할 것**을 나눠서 적었습니다.
제가 대신 못 하는 것은 계정 가입 · 심사 신청 · 키 발급 · 실제 버튼 눌러보기입니다.

---

## 지금 켜져 있는 것 / 꺼져 있는 것

| 항목 | 상태 | 막는 것 |
|---|---|---|
| 구글 로그인 | 키 등록됨 | 구글 콘솔의 [승인된 JavaScript 원본] 확인 필요 |
| 카카오 로그인 | **꺼짐** | `KAKAO_JS_KEY` 가 비어 있음 |
| 토스 실결제 | **꺼짐** | 심사 중 (금요일 전 통과 예정) |
| 문자 발송 | **없음** | 알리고 미가입 |
| 알림톡 | **없음** | 발신프로필 + 템플릿 심사 (4~5일) |

---

## 1. 카카오 로그인 켜기  ★ 오늘 20분이면 끝납니다

코드는 이미 다 돼 있습니다. 서버가 카카오 토큰을 진짜로 검증하고 회원까지 만듭니다.
**키만 넣으면 바로 켜집니다.**

### 대표님이 하실 것

1. https://developers.kakao.com → 로그인 → **내 애플리케이션 → 애플리케이션 추가**
   - 앱 이름 `한국 가상컴` / 사업자명 `한국가상컴`
2. **앱 키** 화면에서 **JavaScript 키** 복사
3. **플랫폼 → Web → 사이트 도메인** 에 아래 추가
   ```
   https://idckorea-dusky.vercel.app
   ```
4. **카카오 로그인 → 활성화 설정 ON**
   - Redirect URI 에도 위 주소를 넣어 두세요 (지금 방식은 안 써도, 나중에 필요합니다)
5. **동의항목** 에서
   - 닉네임 → **필수 동의**
   - 이메일 → **선택 동의** (필수로 하면 이메일 없는 계정이 가입을 못 합니다)
   - 전화번호 → **건드리지 마세요.** 사업자 정보 등록(비즈니스 앱)이 있어야 열리는 항목입니다.
     우리는 **결제할 때 전화번호를 직접 받으므로** 없어도 문제없습니다.
6. Vercel → Settings → Environment Variables 에 추가
   ```
   KAKAO_JS_KEY = (2번에서 복사한 JavaScript 키)
   ```
   → 저장 후 **Redeploy**

### 확인 방법
`/api/health` 에서 `카카오 로그인: ✅ 켜짐` 이 뜨면 된 것입니다.
그다음 로그인 화면에서 실제로 카카오 버튼을 눌러 보세요.

---

## 2. 구글 로그인 확인  ★ 5분

키는 이미 들어가 있습니다. 안 되면 십중팔구 **도메인 등록**이 빠진 것입니다.

### 대표님이 하실 것
1. https://console.cloud.google.com → **API 및 서비스 → 사용자 인증 정보**
2. 쓰고 계신 **OAuth 2.0 클라이언트 ID** 클릭
3. **승인된 JavaScript 원본** 에 아래가 있는지 확인, 없으면 추가
   ```
   https://idckorea-dusky.vercel.app
   ```
4. **OAuth 동의 화면** 이 「테스트」 상태면 **게시(프로덕션)** 로 바꿔 주세요.
   테스트 상태면 등록해 둔 테스트 계정만 로그인됩니다.

---

## 3. 문자 발송 — 알리고  ★ 발신번호 심사가 있어 먼저 신청하셔야 합니다

### 대표님이 하실 것 (먼저)
1. https://smartsms.aligo.in 가입
2. **발신번호 사전등록** — `010-8446-8710`
   - 사업자등록증 등 서류 심사가 있습니다. **신청부터 하세요.**
   - 등록 안 된 번호로는 발송 자체가 막힙니다 (통신사 정책)
3. 충전 (문자 1건 약 8~20원)
4. **API 키** 발급 → Vercel 환경변수 3개
   ```
   ALIGO_KEY     = (발급받은 API 키)
   ALIGO_USER_ID = (알리고 아이디)
   ALIGO_SENDER  = 01084468710
   ```

### 제가 할 것
- `api/_notify.js` — 알리고 발송 창구
  - 90바이트 넘으면 자동으로 LMS 로 넘어갑니다
  - 알리고는 EUC-KR 이라 이모지가 `?` 로 깨집니다. 문구에서 뺍니다.
- 자동 발송 세 군데를 실제 발송으로 바꿉니다
  1. **결제 완료 직후** — 품번 · 애니데스크 번호 · 비밀번호 · 이용기간
     (※ 서버실 위치는 절대 안 넣습니다)
  2. **만료 3일 전** — 연장 안내
  3. **관리자 [알림 발송]** 화면 — 지금은 "실서비스에서는…" 안내창만 뜨는 목업입니다
- 발송 기록을 `view_log` 에 남깁니다 (누구에게 언제 무엇을 보냈는지)
- 발송 실패 시 관리자 화면에 빨갛게 표시

---

## 4. 알림톡  ★ 템플릿 심사 4~5일 — 문자보다 오래 걸립니다

문자보다 싸고(약 6~9원) 카카오톡으로 가서 열람률이 높습니다.
다만 **정해진 템플릿**으로만 보낼 수 있고 심사를 받아야 합니다.

### 대표님이 하실 것
1. 카카오톡 채널 **「한국 가상컴」** (이미 있음 — `pf.kakao.com/_GxcgFxj`) 을
   알리고에 **발신프로필**로 등록
2. 템플릿 등록 · 심사 신청 (아래 문안은 제가 만들어 드립니다)
3. 심사 **4~5일** 소요 — 반려되면 문구를 고쳐 재신청

### 제가 할 것
- 템플릿 문안 3종 초안 작성 (결제완료 / 만료예정 / 연장완료)
- **알림톡 먼저 보내고, 실패하면 문자로 자동 전환** 하는 코드
  (친구 추가가 안 된 고객에게는 알림톡이 안 갑니다. 그래서 전환이 꼭 필요합니다)

---

## 5. 토스 실결제 열기  ★ 심사 통과 후 10분

심사 통과하면 **키 두 개**를 Vercel 에 넣으시면 됩니다. 코드는 안 고쳐도 됩니다.

```
TOSS_SECRET_KEY = live_sk_...     (서버용 · 절대 공개 금지)
TOSS_CLIENT_KEY = live_ck_...     (브라우저용 · 공개돼도 안전)
```

토스 개발자센터 → 내 개발정보 → API 키 에서 둘 다 복사하실 수 있습니다.

> **둘 다 바꿔야 합니다.** 시크릿만 라이브로 바꾸고 클라이언트를 테스트로 두면
> 결제창은 테스트로 뜨는데 승인은 라이브로 가서 어긋납니다.
> `/api/health` 에서 두 줄 다 `✅ 실결제 키` 인지 확인하세요.

### 통과 직후 반드시 하실 것
- **본인 카드로 1건 실제 결제** → 관리자에서 PC 자동배정 확인 → **즉시 취소**
- 취소가 정상 처리되는지까지 봐야 합니다 (환불 경로가 실제로 도는지)

---

## 6. 보안 — 지금 바로

- **관리자 비밀번호가 10자 미만입니다.** 이거 하나로 관리자 화면 전체가 열립니다.
  Vercel `KVC_ADMIN_PW` 를 길게 바꿔 주세요.

---

## 7. 데이터베이스 (남은 SQL 하나)

- `db/07-만료정리.sql` — 기간 끝난 주문의 PC 를 판매 재고로 되돌립니다.
  안 돌리면 30일 뒤부터 재고가 계속 줄어듭니다. 급하지는 않지만 **첫 결제 전에는** 하세요.

> `db/02` · `03` · `04` 는 **다시 돌리지 마세요.** 옛 beat 함수가 들어 있어
> 06-B 가 되돌아갑니다. 파일 맨 위에 경고를 붙여 뒀습니다.

---

## 8. 그 뒤 (급하지 않음)

- 관리자 대시보드 → [오늘 할 일] 중심으로 정리
- 주문 달력 검색
- 자동 연장 (정기결제)
- 세금계산서 발행 연동

---

## 순서 제안

심사가 걸린 것부터 **먼저 신청**해 두고, 기다리는 동안 나머지를 하는 게 빠릅니다.

1. **오늘** — 알리고 가입 + 발신번호 등록 신청 (심사 있음)
2. **오늘** — 알림톡 발신프로필 + 템플릿 심사 신청 (4~5일)
3. **오늘** — 카카오 로그인 키 (20분) · 구글 도메인 확인 (5분) · 관리자 비밀번호
4. **내일 현장** — 랙PC 세팅 + 등록
5. **토스 통과하면** — 키 두 개 넣고 실결제 1건 테스트
6. **알리고 승인되면** — 제가 문자 발송 코드 올림
7. **알림톡 승인되면** — 알림톡 우선 + 문자 전환
